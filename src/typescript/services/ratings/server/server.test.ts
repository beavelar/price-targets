import { RatingsServer } from './server';

describe('RatingsServer test suite', () => {
  test('place holder test', () => {
    const server = new RatingsServer('');
    expect(server).toBeDefined();
  });
});